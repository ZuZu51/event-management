import { useParams, useNavigate } from 'react-router-dom';
import { EventDetailTabs } from './EventDetail';

const DetailEventWrapper = () => {
  return <EventDetailTabs />;
};

export default DetailEventWrapper;
